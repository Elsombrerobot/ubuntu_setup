Modify zshrc to execute .zsh file from my conf
```
for conf in "$HOME/.marc/"*.zsh; do
   source "${conf}"
done
unset conf
```

Install plugins in `vim`

Set wallpapers with `shotwell`.

4Restart `zsh` and execute `updatefull`, `sourceterm`

Change term shortcut to `ctrl+shift+te`
