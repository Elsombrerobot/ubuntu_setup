- Excute to install and setup all `wget -O - https://raw.githubusercontent.com/Elsombrerobot/ubuntu_setup/master/ubuntu_setup.sh | sudo bash`

- Launch zsh and choose option 2

- Modify .zshrc to execute .zsh file from my conf
```
for conf in "$HOME/.marc/"*.zsh; do
   source "${conf}"
done
unset conf
```

- Install plugins in `vim` : `:PlugInstall`

- Set wallpapers with `shotwell`.

- Restart `zsh` and execute `updatefull`, `sourceterm`
